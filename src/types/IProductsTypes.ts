export interface IProductsJSON {
    productsJSON: {
        availableCoupon?: null | boolean,
        coverImage: string,
        id: string,
        price: number,
        score: number,
        title: string,
        count: number,
        isChecked: boolean,
        isInWishList: boolean,
    }
}